<div>
  <!-- <h6>JANELLE UY</h6> -->

  <nav>
    <a id="home" href="/jcu25.github.io/">Home</a>
    <a id="projets" href="/jcu25.github.io/projects">My Projects</a>
    <a id="about-me" href="/jcu25.github.io/about-me">About Me</a>
  </nav>
</div>

<script>
  const home: HTMLElement | null = document.getElementById("home");
  const projects: HTMLElement | null = document.getElementById("projets");
  const about_me: HTMLElement | null = document.getElementById("about-me");

  console.log(window.location.pathname);

  // text decoration when "Home" is selected
  if (window.location.pathname == "/") {
    if (home) {
      home.style.textDecoration = "underline";
    }
  }

  // text decoration when "Projects" is selected
  if (
    window.location.pathname == "/jcu25.github.io/projects" ||
    window.location.pathname == "/jcu25.github.io/projects/"
  ) {
    if (projects) {
      projects.style.textDecoration = "underline";
    }
  }

  // text decoration when "About Me" is selected
  if (
    window.location.pathname == "/about-me" ||
    window.location.pathname == "/about-me/"
  ) {
    if (about_me) {
      about_me.style.textDecoration = "underline";
    }
  }
</script>

<style>
  div {
    margin: 0;
    background-color: #ffffff;
    width: 99vw;
    height: 70px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-bottom: 1px solid #262e6e;

    position: fixed;
    top: 0;
  }

  nav {
    width: 40%;
    background-color: #ffffff;
    height: 100%;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }

  a {
    text-decoration: none;
    color: #000000;
    font-size: 18px;
    font-family: "Manrope", sans-serif;
    font-weight: 600;
  }

  a:hover {
    text-decoration: underline;
    transform: scale(1.05);
  }

  @media screen and (max-width: 1200px) {
    div {
      margin: 0;
      background-color: #ffffff;
      width: 100vw;
      height: 56px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    nav {
      width: 60%;
      background-color: #ffffff;
      height: 100%;
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }

    a {
      text-decoration: none;
      color: #000000;
      font-size: 15px;
      font-family: "Raleway", sans-serif;
      font-weight: 600;
    }

    a:hover {
      text-decoration: underline;
    }
  }

  @media screen and (max-width: 600px) {
    div {
      margin: 0;
      background-color: #ffffff;
      width: 100vw;
      height: 56px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    nav {
      width: 90vw;
      background-color: #ffffff;
      height: 100%;
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }

    a {
      text-decoration: none;
      color: #000000;
      font-size: 15px;
      font-family: "Raleway", sans-serif;
      font-weight: 600;
    }

    a:hover {
      text-decoration: underline;
    }
  }
</style>
